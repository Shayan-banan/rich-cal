<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>WealthAge™ — When Will You Get Rich?</title>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;900&family=Lato:wght@300;400;700&family=Space+Mono&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/4.4.1/chart.umd.min.js"></script>
<style>
  :root {
    --gold: #c9a84c;
    --gold-light: #e8c96a;
    --gold-dark: #8a6f2e;
    --bg: #07070e;
    --bg2: #0e0e1a;
    --bg3: #141428;
    --text: #f0ead6;
    --text-muted: #8a8070;
    --green: #2ecc71;
    --red: #e74c3c;
    --border: rgba(201,168,76,0.2);
  }

  * { margin:0; padding:0; box-sizing:border-box; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Lato', sans-serif;
    overflow-x: hidden;
    cursor: default;
  }

  /* ─── CANVAS PARTICLES ─── */
  #particles {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: 0;
    pointer-events: none;
  }

  /* ─── GLOBAL LAYOUT ─── */
  section { position: relative; z-index: 1; }

  .gold-line {
    width: 60px; height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 0 auto 24px;
  }

  /* ─── HERO ─── */
  #hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 40px 20px;
    position: relative;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    border: 1px solid var(--border);
    padding: 6px 18px;
    border-radius: 100px;
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--gold);
    letter-spacing: 2px;
    margin-bottom: 40px;
    animation: fadeDown 1s ease both;
  }

  .badge::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--gold);
    border-radius: 50%;
    animation: pulse 1.5s ease infinite;
  }

  @keyframes pulse {
    0%,100% { opacity:1; transform:scale(1); }
    50% { opacity:0.4; transform:scale(0.6); }
  }

  #hero h1 {
    font-family: 'Cinzel', serif;
    font-size: clamp(2.8rem, 7vw, 6rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -1px;
    margin-bottom: 24px;
    animation: fadeDown 1s 0.2s ease both;
  }

  #hero h1 .accent {
    background: linear-gradient(135deg, var(--gold-light), var(--gold), var(--gold-dark));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  #hero p.sub {
    max-width: 580px;
    font-size: 1.15rem;
    color: var(--text-muted);
    line-height: 1.8;
    margin-bottom: 48px;
    animation: fadeDown 1s 0.4s ease both;
  }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: linear-gradient(135deg, var(--gold), var(--gold-dark));
    color: #07070e;
    font-family: 'Cinzel', serif;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 1px;
    padding: 18px 44px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    animation: fadeDown 1s 0.6s ease both;
    position: relative;
    overflow: hidden;
  }

  .cta-btn::before {
    content: '';
    position: absolute;
    top: 0; left: -100%;
    width: 100%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
    transition: left 0.5s ease;
  }

  .cta-btn:hover::before { left: 100%; }
  .cta-btn:hover { transform: translateY(-2px); box-shadow: 0 20px 60px rgba(201,168,76,0.3); }

  .stats-strip {
    display: flex;
    gap: 60px;
    margin-top: 80px;
    animation: fadeUp 1s 0.8s ease both;
  }

  .stat-item { text-align: center; }
  .stat-num {
    font-family: 'Cinzel', serif;
    font-size: 2rem;
    color: var(--gold);
    display: block;
  }
  .stat-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    animation: fadeDown 1s 1.2s ease both;
  }

  .scroll-indicator span {
    font-size: 0.7rem;
    letter-spacing: 3px;
    color: var(--text-muted);
    text-transform: uppercase;
  }

  .scroll-line {
    width: 1px; height: 50px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    animation: scrollPulse 2s ease infinite;
  }

  @keyframes scrollPulse {
    0% { transform: scaleY(1); opacity:1; }
    50% { transform: scaleY(0.5); opacity:0.4; }
    100% { transform: scaleY(1); opacity:1; }
  }

  /* ─── QUIZ SECTION ─── */
  #quiz {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 80px 20px;
  }

  .quiz-shell {
    width: 100%;
    max-width: 720px;
  }

  .progress-bar-wrap {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 48px;
  }

  .progress-track {
    flex: 1;
    height: 3px;
    background: rgba(255,255,255,0.06);
    border-radius: 10px;
    overflow: hidden;
  }

  .progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--gold-dark), var(--gold-light));
    border-radius: 10px;
    transition: width 0.6s cubic-bezier(0.4,0,0.2,1);
  }

  .progress-label {
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--gold);
    white-space: nowrap;
  }

  .quiz-step {
    display: none;
    animation: stepIn 0.5s ease both;
  }

  .quiz-step.active { display: block; }

  @keyframes stepIn {
    from { opacity:0; transform:translateX(30px); }
    to { opacity:1; transform:translateX(0); }
  }

  .step-label {
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    color: var(--gold);
    letter-spacing: 3px;
    text-transform: uppercase;
    margin-bottom: 16px;
  }

  .step-q {
    font-family: 'Cinzel', serif;
    font-size: clamp(1.4rem, 3.5vw, 2.2rem);
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 10px;
  }

  .step-hint {
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 40px;
    line-height: 1.6;
  }

  /* Input styles */
  .input-group {
    margin-bottom: 24px;
  }

  .input-group label {
    display: block;
    font-size: 0.8rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--text-muted);
    margin-bottom: 10px;
  }

  .input-group input,
  .input-group select {
    width: 100%;
    background: rgba(255,255,255,0.04);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 16px 20px;
    color: var(--text);
    font-family: 'Lato', sans-serif;
    font-size: 1rem;
    outline: none;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
  }

  .input-group input:focus,
  .input-group select:focus {
    border-color: var(--gold);
    box-shadow: 0 0 0 3px rgba(201,168,76,0.12);
  }

  .input-group select option { background: var(--bg2); }

  /* Slider */
  .slider-wrap {
    position: relative;
    padding: 20px 0;
  }

  input[type=range] {
    -webkit-appearance: none;
    width: 100%;
    height: 3px;
    background: rgba(255,255,255,0.1);
    border-radius: 10px;
    outline: none;
  }

  input[type=range]::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 22px; height: 22px;
    background: var(--gold);
    border-radius: 50%;
    cursor: pointer;
    box-shadow: 0 0 20px rgba(201,168,76,0.5);
    transition: transform 0.2s ease;
  }

  input[type=range]::-webkit-slider-thumb:hover { transform: scale(1.2); }

  .slider-value {
    font-family: 'Cinzel', serif;
    font-size: 2.5rem;
    color: var(--gold);
    margin-bottom: 12px;
    display: block;
  }

  .slider-labels {
    display: flex;
    justify-content: space-between;
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-top: 10px;
  }

  /* Choice cards */
  .choice-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 12px;
    margin-bottom: 8px;
  }

  .choice-card {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 18px 20px;
    cursor: pointer;
    transition: all 0.25s ease;
    background: rgba(255,255,255,0.02);
    position: relative;
    overflow: hidden;
  }

  .choice-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(201,168,76,0.1), transparent);
    opacity: 0;
    transition: opacity 0.25s ease;
  }

  .choice-card:hover { border-color: rgba(201,168,76,0.5); transform:translateY(-2px); }
  .choice-card:hover::before { opacity:1; }

  .choice-card.selected {
    border-color: var(--gold);
    background: rgba(201,168,76,0.08);
  }
  .choice-card.selected::before { opacity:1; }

  .choice-card .icon { font-size: 1.8rem; margin-bottom: 10px; }
  .choice-card .label { font-size: 0.9rem; font-weight: 700; margin-bottom: 4px; }
  .choice-card .desc { font-size: 0.75rem; color: var(--text-muted); }

  /* Psych questions */
  .psych-q {
    margin-bottom: 36px;
    padding-bottom: 36px;
    border-bottom: 1px solid rgba(255,255,255,0.04);
  }

  .psych-q:last-child { border-bottom: none; }

  .psych-q .qtext {
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 16px;
  }

  .psych-scale {
    display: flex;
    gap: 8px;
  }

  .scale-btn {
    flex: 1;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--text);
    padding: 12px 4px;
    border-radius: 6px;
    font-family: 'Space Mono', monospace;
    font-size: 0.85rem;
    cursor: pointer;
    transition: all 0.2s ease;
    text-align: center;
  }

  .scale-btn:hover { border-color: var(--gold); color: var(--gold); }
  .scale-btn.selected { background: var(--gold); border-color: var(--gold); color: var(--bg); font-weight: 700; }

  .scale-ends {
    display: flex;
    justify-content: space-between;
    font-size: 0.7rem;
    color: var(--text-muted);
    margin-top: 6px;
  }

  /* Navigation */
  .nav-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 48px;
  }

  .btn-back {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--text-muted);
    padding: 14px 28px;
    border-radius: 4px;
    font-family: 'Lato', sans-serif;
    font-size: 0.9rem;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .btn-back:hover { border-color: var(--text-muted); color: var(--text); }

  .btn-next {
    background: linear-gradient(135deg, var(--gold), var(--gold-dark));
    border: none;
    color: #07070e;
    padding: 16px 48px;
    border-radius: 4px;
    font-family: 'Cinzel', serif;
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 1px;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .btn-next:hover { transform:translateY(-2px); box-shadow: 0 15px 40px rgba(201,168,76,0.25); }

  /* ─── RESULTS ─── */
  #results {
    display: none;
    min-height: 100vh;
    padding: 80px 20px;
  }

  #results.visible { display: block; }

  .results-container {
    max-width: 960px;
    margin: 0 auto;
  }

  .age-reveal {
    text-align: center;
    margin-bottom: 80px;
  }

  .age-reveal .eyebrow {
    font-family: 'Space Mono', monospace;
    font-size: 11px;
    letter-spacing: 4px;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 24px;
  }

  .age-circle {
    width: 240px; height: 240px;
    border-radius: 50%;
    border: 2px solid var(--border);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 0 auto 32px;
    position: relative;
    background: radial-gradient(circle, rgba(201,168,76,0.08) 0%, transparent 70%);
  }

  .age-circle::before {
    content: '';
    position: absolute;
    inset: -10px;
    border-radius: 50%;
    border: 1px solid rgba(201,168,76,0.1);
    animation: agePulse 3s ease infinite;
  }

  .age-circle::after {
    content: '';
    position: absolute;
    inset: -20px;
    border-radius: 50%;
    border: 1px solid rgba(201,168,76,0.05);
    animation: agePulse 3s 0.5s ease infinite;
  }

  @keyframes agePulse {
    0%,100% { transform:scale(1); opacity:1; }
    50% { transform:scale(1.05); opacity:0; }
  }

  .age-number {
    font-family: 'Cinzel', serif;
    font-size: 5rem;
    font-weight: 900;
    color: var(--gold);
    line-height: 1;
    display: block;
  }

  .age-sub { font-size: 0.85rem; color: var(--text-muted); }

  .result-headline {
    font-family: 'Cinzel', serif;
    font-size: clamp(1.5rem, 3vw, 2.2rem);
    margin-bottom: 16px;
  }

  .result-desc {
    color: var(--text-muted);
    max-width: 560px;
    margin: 0 auto;
    line-height: 1.8;
  }

  /* Factor cards */
  .factors-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 16px;
    margin: 60px 0;
  }

  .factor-card {
    background: rgba(255,255,255,0.025);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 28px 24px;
    position: relative;
    overflow: hidden;
    animation: cardReveal 0.6s ease both;
  }

  @keyframes cardReveal {
    from { opacity:0; transform:translateY(20px); }
    to { opacity:1; transform:translateY(0); }
  }

  .factor-card .card-icon { font-size: 2rem; margin-bottom: 12px; }
  .factor-card .card-title { font-size: 0.75rem; letter-spacing: 2px; text-transform: uppercase; color: var(--text-muted); margin-bottom: 8px; }
  .factor-card .card-value { font-family: 'Cinzel', serif; font-size: 1.4rem; color: var(--text); }
  .factor-card .card-impact { font-size: 0.75rem; margin-top: 8px; }
  .factor-card .card-impact.pos { color: var(--green); }
  .factor-card .card-impact.neg { color: var(--red); }
  .factor-card .card-impact.neu { color: var(--gold); }

  .factor-card::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    height: 3px; width: 100%;
    background: linear-gradient(90deg, var(--gold-dark), var(--gold-light));
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.5s ease;
  }

  .factor-card:hover::after { transform: scaleX(1); }

  /* Chart */
  .chart-section {
    background: rgba(255,255,255,0.02);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 40px;
    margin: 40px 0;
  }

  .chart-title {
    font-family: 'Cinzel', serif;
    font-size: 1.3rem;
    margin-bottom: 8px;
  }

  .chart-subtitle {
    color: var(--text-muted);
    font-size: 0.85rem;
    margin-bottom: 32px;
  }

  /* Mindset score */
  .mindset-section {
    background: linear-gradient(135deg, rgba(201,168,76,0.06), rgba(201,168,76,0.02));
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 40px;
    margin: 40px 0;
  }

  .mindset-score-bar {
    height: 8px;
    background: rgba(255,255,255,0.06);
    border-radius: 10px;
    overflow: hidden;
    margin: 20px 0 8px;
  }

  .mindset-fill {
    height: 100%;
    border-radius: 10px;
    background: linear-gradient(90deg, var(--gold-dark), var(--gold-light));
    transition: width 1.5s 0.5s cubic-bezier(0.4,0,0.2,1);
  }

  .mindset-traits {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 24px;
  }

  .trait-tag {
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 6px 16px;
    font-size: 0.78rem;
    color: var(--gold);
  }

  /* Science section */
  .science-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin: 40px 0;
  }

  @media (max-width: 600px) {
    .science-grid { grid-template-columns: 1fr; }
    .stats-strip { gap: 30px; flex-wrap:wrap; }
  }

  .science-card {
    background: rgba(255,255,255,0.02);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 28px;
  }

  .science-card .src {
    font-family: 'Space Mono', monospace;
    font-size: 10px;
    color: var(--gold-dark);
    letter-spacing: 1px;
    margin-bottom: 12px;
  }

  .science-card .stat-big {
    font-family: 'Cinzel', serif;
    font-size: 2.4rem;
    color: var(--gold);
    line-height: 1;
    margin-bottom: 12px;
  }

  .science-card p {
    font-size: 0.88rem;
    color: var(--text-muted);
    line-height: 1.7;
  }

  .restart-btn {
    display: block;
    margin: 60px auto 0;
    background: transparent;
    border: 1px solid var(--border);
    color: var(--text);
    font-family: 'Cinzel', serif;
    font-size: 0.9rem;
    letter-spacing: 2px;
    padding: 16px 40px;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .restart-btn:hover { border-color: var(--gold); color: var(--gold); }

  /* ─── ANIMATIONS ─── */
  @keyframes fadeDown {
    from { opacity:0; transform:translateY(-20px); }
    to { opacity:1; transform:translateY(0); }
  }

  @keyframes fadeUp {
    from { opacity:0; transform:translateY(20px); }
    to { opacity:1; transform:translateY(0); }
  }

  /* ─── SECTION DIVIDER ─── */
  .section-head {
    text-align: center;
    margin-bottom: 60px;
  }

  .section-head h2 {
    font-family: 'Cinzel', serif;
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    font-weight: 600;
    margin-bottom: 16px;
  }

  .section-head p {
    color: var(--text-muted);
    font-size: 0.95rem;
    max-width: 500px;
    margin: 0 auto;
    line-height: 1.8;
  }

  /* Validation error */
  .error-msg {
    color: var(--red);
    font-size: 0.8rem;
    margin-top: 8px;
    display: none;
  }

  .input-group.error input,
  .input-group.error select {
    border-color: var(--red);
  }

  /* ─── TWO COL ─── */
  .two-col {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }

  @media (max-width: 560px) { .two-col { grid-template-columns: 1fr; } }

  /* Floating orbs for ambiance */
  .orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.07;
    pointer-events: none;
    z-index: 0;
  }

  .orb1 { width:600px; height:600px; background:var(--gold); top:-200px; right:-200px; animation: orbFloat 15s ease infinite; }
  .orb2 { width:400px; height:400px; background:#2980b9; bottom:-100px; left:-100px; animation: orbFloat 20s 5s ease infinite; }

  @keyframes orbFloat {
    0%,100% { transform:translate(0,0) scale(1); }
    33% { transform:translate(30px,-30px) scale(1.05); }
    66% { transform:translate(-20px,20px) scale(0.97); }
  }
</style>
</head>
<body>

<!-- Background orbs -->
<div class="orb orb1"></div>
<div class="orb orb2"></div>

<!-- Particle canvas -->
<canvas id="particles"></canvas>

<!-- ───── HERO ───── -->
<section id="hero">
  <div class="badge">✦ Backed by behavioral finance & cognitive science</div>

  <h1>When Will You<br><span class="accent">Get Rich?</span></h1>

  <p class="sub">
    Answer 5 minutes of science-backed questions about your habits,
    mindset, and situation. We'll calculate your exact wealth age using
    compound growth models and behavioral psychology.
  </p>

  <button class="cta-btn" onclick="startQuiz()">
    ✦ Calculate My Wealth Age
  </button>

  <div class="stats-strip">
    <div class="stat-item">
      <span class="stat-num" id="counter1">0</span>
      <span class="stat-label">Avg. Millionaire Age</span>
    </div>
    <div class="stat-item">
      <span class="stat-num" id="counter2">0%</span>
      <span class="stat-label">Self-Made Millionaires</span>
    </div>
    <div class="stat-item">
      <span class="stat-num" id="counter3">0x</span>
      <span class="stat-label">Compound Growth Impact</span>
    </div>
  </div>

  <div class="scroll-indicator">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- ───── QUIZ ───── -->
<section id="quiz">
  <div class="quiz-shell">
    <div class="progress-bar-wrap">
      <div class="progress-track">
        <div class="progress-fill" id="progressFill" style="width:0%"></div>
      </div>
      <span class="progress-label" id="progressLabel">STEP 1 / 5</span>
    </div>

    <!-- STEP 1: Definition of Rich -->
    <div class="quiz-step active" id="step1">
      <div class="step-label">Step 01 — Your Goal</div>
      <h2 class="step-q">What does "rich" mean to you?</h2>
      <p class="step-hint">Drag the slider to your target net worth. There's no wrong answer — this calibrates your wealth trajectory model.</p>

      <div class="slider-wrap">
        <span class="slider-value" id="richVal">$1,000,000</span>
        <input type="range" id="richSlider" min="0" max="100" value="40"
               oninput="updateRichSlider()">
        <div class="slider-labels">
          <span>$100K</span>
          <span>$1M</span>
          <span>$10M</span>
          <span>$100M</span>
          <span>$1B</span>
        </div>
      </div>

      <div class="choice-grid" style="margin-top:32px">
        <div class="choice-card" data-key="richType" data-val="freedom" onclick="selectCard(this)">
          <div class="icon">🌊</div>
          <div class="label">Freedom</div>
          <div class="desc">Never work again. Choose everything.</div>
        </div>
        <div class="choice-card" data-key="richType" data-val="security" onclick="selectCard(this)">
          <div class="icon">🏰</div>
          <div class="label">Security</div>
          <div class="desc">No financial anxiety ever.</div>
        </div>
        <div class="choice-card" data-key="richType" data-val="status" onclick="selectCard(this)">
          <div class="icon">👑</div>
          <div class="label">Status</div>
          <div class="desc">Be recognized as wealthy.</div>
        </div>
        <div class="choice-card" data-key="richType" data-val="impact" onclick="selectCard(this)">
          <div class="icon">🌍</div>
          <div class="label">Impact</div>
          <div class="desc">Build something that matters.</div>
        </div>
      </div>

      <div class="nav-row">
        <div></div>
        <button class="btn-next" onclick="nextStep(1)">Continue →</button>
      </div>
    </div>

    <!-- STEP 2: Who Are You -->
    <div class="quiz-step" id="step2">
      <div class="step-label">Step 02 — Your Profile</div>
      <h2 class="step-q">Tell us about yourself</h2>
      <p class="step-hint">Your baseline. The algorithm needs your current coordinates to plot the route.</p>

      <div class="two-col">
        <div class="input-group" id="ageGroup">
          <label>Your Current Age</label>
          <input type="number" id="currentAge" placeholder="e.g. 28" min="16" max="80">
          <div class="error-msg" id="ageErr">Please enter a valid age (16–80)</div>
        </div>
        <div class="input-group">
          <label>Current Savings / Net Worth ($)</label>
          <input type="number" id="currentSavings" placeholder="e.g. 5000">
        </div>
      </div>

      <div class="input-group">
        <label>Monthly Income (after tax)</label>
        <input type="number" id="monthlyIncome" placeholder="e.g. 3500">
      </div>

      <div class="input-group">
        <label>Monthly Expenses</label>
        <input type="number" id="monthlyExpenses" placeholder="e.g. 2500">
      </div>

      <div class="input-group">
        <label>Primary Occupation</label>
        <select id="occupation">
          <option value="">— Select —</option>
          <option value="tech">Tech / Engineering</option>
          <option value="finance">Finance / Banking</option>
          <option value="business">Business / Entrepreneur</option>
          <option value="creative">Creative / Media</option>
          <option value="healthcare">Healthcare / Medicine</option>
          <option value="education">Education</option>
          <option value="trades">Trades / Skilled Labour</option>
          <option value="service">Service / Hospitality</option>
          <option value="law">Law / Consulting</option>
          <option value="other">Other</option>
        </select>
      </div>

      <div class="nav-row">
        <button class="btn-back" onclick="prevStep(2)">← Back</button>
        <button class="btn-next" onclick="nextStep(2)">Continue →</button>
      </div>
    </div>

    <!-- STEP 3: Location & Lifestyle -->
    <div class="quiz-step" id="step3">
      <div class="step-label">Step 03 — Location & Lifestyle</div>
      <h2 class="step-q">Where and how do you live?</h2>
      <p class="step-hint">Geography dramatically alters wealth trajectories. A $60K salary in Austin ≠ $60K in Manhattan.</p>

      <div class="input-group">
        <label>Cost of Living</label>
        <select id="colIndex">
          <option value="">— Select your city type —</option>
          <option value="1.8">Tier 1 — NYC, SF, London, Zurich</option>
          <option value="1.4">Tier 2 — LA, Chicago, Austin, Toronto</option>
          <option value="1.0">Tier 3 — Mid-size US/EU city</option>
          <option value="0.7">Tier 4 — Smaller city / rural US/EU</option>
          <option value="0.5">Tier 5 — Developing world / geo-arbitrage</option>
        </select>
      </div>

      <p style="font-size:0.85rem; color:var(--text-muted); margin-bottom:20px;">How do you handle money?</p>

      <div class="choice-grid">
        <div class="choice-card" data-key="lifestyle" data-val="frugal" onclick="selectCard(this)">
          <div class="icon">🧘</div>
          <div class="label">Frugal</div>
          <div class="desc">Spend deliberately. Live below means.</div>
        </div>
        <div class="choice-card" data-key="lifestyle" data-val="balanced" onclick="selectCard(this)">
          <div class="icon">⚖️</div>
          <div class="label">Balanced</div>
          <div class="desc">Enjoy life, still save.</div>
        </div>
        <div class="choice-card" data-key="lifestyle" data-val="lifestyle_creep" onclick="selectCard(this)">
          <div class="icon">📈</div>
          <div class="label">Lifestyle Creep</div>
          <div class="desc">Spend rises with income.</div>
        </div>
        <div class="choice-card" data-key="lifestyle" data-val="yolo" onclick="selectCard(this)">
          <div class="icon">🎰</div>
          <div class="label">YOLO</div>
          <div class="desc">You only live once.</div>
        </div>
      </div>

      <div class="input-group" style="margin-top:24px">
        <label>Investment Strategy</label>
        <select id="investmentType">
          <option value="">— Select —</option>
          <option value="0.04">Conservative — Bonds/CDs (~4%)</option>
          <option value="0.08">Moderate — Index funds (~8%)</option>
          <option value="0.12">Growth — ETFs + stocks (~12%)</option>
          <option value="0.18">Aggressive — Crypto/startups (~18%)</option>
          <option value="0.0">None — Cash savings (0%)</option>
        </select>
      </div>

      <div class="nav-row">
        <button class="btn-back" onclick="prevStep(3)">← Back</button>
        <button class="btn-next" onclick="nextStep(3)">Continue →</button>
      </div>
    </div>

    <!-- STEP 4: Psychological Test -->
    <div class="quiz-step" id="step4">
      <div class="step-label">Step 04 — Wealth Psychology</div>
      <h2 class="step-q">Your Wealth Mindset Assessment</h2>
      <p class="step-hint">Based on Carol Dweck's growth mindset research, Kahneman's behavioral economics, and Mischel's delayed gratification studies. Rate 1–5 honestly.</p>

      <div class="psych-q">
        <p class="qtext">1. If you had $500 right now, what's most likely to happen to it within 30 days?</p>
        <div class="psych-scale" data-q="q1">
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">Spent on fun</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Bills / needs</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Saved</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">Invested</button>
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">Multiplied it</button>
        </div>
      </div>

      <div class="psych-q">
        <p class="qtext">2. When you fail financially (bad investment, overspend), your typical reaction:</p>
        <div class="psych-scale" data-q="q2">
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">Spiral/avoid</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Blame others</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Forget it</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">Analyze it</button>
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">Iterate fast</button>
        </div>
      </div>

      <div class="psych-q">
        <p class="qtext">3. How often do you think about growing your income vs cutting expenses?</p>
        <div class="psych-scale" data-q="q3">
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">Never</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Rarely</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Sometimes</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">Often</button>
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">Obsessively</button>
        </div>
      </div>

      <div class="psych-q">
        <p class="qtext">4. Your relationship with delayed gratification (waiting for bigger rewards):</p>
        <div class="psych-scale" data-q="q4">
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">I can't wait</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Hard for me</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Neutral</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">I'm patient</button>
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">I'm very patient</button>
        </div>
      </div>

      <div class="psych-q">
        <p class="qtext">5. How much does peer spending (friends, social media) influence your purchases?</p>
        <div class="psych-scale" data-q="q5">
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">Not at all</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">Rarely</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Sometimes</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Often</button>
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">Constantly</button>
        </div>
        <div class="scale-ends"><span>Strongest mindset</span><span>Weakest mindset</span></div>
      </div>

      <div class="psych-q">
        <p class="qtext">6. I actively learn about investing, business, and wealth-building:</p>
        <div class="psych-scale" data-q="q6">
          <button class="scale-btn" data-score="1" onclick="selectScale(this)">Never</button>
          <button class="scale-btn" data-score="2" onclick="selectScale(this)">Rarely</button>
          <button class="scale-btn" data-score="3" onclick="selectScale(this)">Monthly</button>
          <button class="scale-btn" data-score="4" onclick="selectScale(this)">Weekly</button>
          <button class="scale-btn" data-score="5" onclick="selectScale(this)">Daily</button>
        </div>
      </div>

      <div class="nav-row">
        <button class="btn-back" onclick="prevStep(4)">← Back</button>
        <button class="btn-next" onclick="nextStep(4)">Calculate My Age →</button>
      </div>
    </div>
  </div>
</section>

<!-- ───── RESULTS ───── -->
<section id="results">
  <div class="results-container">

    <div class="age-reveal">
      <div class="eyebrow">✦ YOUR WEALTH AGE ANALYSIS</div>
      <div class="age-circle">
        <span class="age-number" id="resultAge">—</span>
        <span class="age-sub">years old</span>
      </div>
      <h2 class="result-headline" id="resultHeadline">You'll reach your wealth goal at age <span id="inlineAge" style="color:var(--gold)">—</span></h2>
      <p class="result-desc" id="resultDesc">Calculating your trajectory...</p>
    </div>

    <div class="gold-line"></div>

    <div class="section-head">
      <h2>Your Wealth Breakdown</h2>
      <p>Every factor impacting your trajectory, ranked by influence.</p>
    </div>

    <div class="factors-grid" id="factorsGrid"></div>

    <!-- Wealth chart -->
    <div class="chart-section">
      <div class="chart-title">Net Worth Projection</div>
      <div class="chart-subtitle">Your compound growth curve vs. average trajectory</div>
      <canvas id="wealthChart" height="120"></canvas>
    </div>

    <!-- Mindset score -->
    <div class="mindset-section">
      <div class="section-head" style="margin-bottom:24px; text-align:left">
        <h2>Your Wealth Mindset Score</h2>
        <p style="text-align:left">Based on Dweck (2006) growth mindset framework and Kahneman (2011) behavioral economics</p>
      </div>
      <div class="mindset-score-bar">
        <div class="mindset-fill" id="mindsetFill" style="width:0%"></div>
      </div>
      <div style="display:flex; justify-content:space-between; font-size:0.75rem; color:var(--text-muted)">
        <span>Fixed Mindset</span>
        <span id="mindsetScore" style="color:var(--gold); font-family:'Space Mono',monospace"></span>
        <span>Wealth Mindset</span>
      </div>
      <div class="mindset-traits" id="mindsetTraits"></div>
    </div>

    <!-- Science stats -->
    <div class="section-head">
      <h2>The Science Behind the Numbers</h2>
      <p>Your results are grounded in peer-reviewed research and real wealth data.</p>
    </div>

    <div class="science-grid">
      <div class="science-card">
        <div class="src">FEDERAL RESERVE — SURVEY OF CONSUMER FINANCES, 2023</div>
        <div class="stat-big">57</div>
        <p>The average age a person first reaches millionaire status in the United States. Those who invest early consistently beat this by 10–15 years.</p>
      </div>
      <div class="science-card">
        <div class="src">FIDELITY INVESTMENTS — MILLIONAIRE OUTLOOK, 2023</div>
        <div class="stat-big">88%</div>
        <p>Of millionaires are self-made — not from inheritance. The #1 shared trait: consistent, automated investing that began before age 35.</p>
      </div>
      <div class="science-card">
        <div class="src">MISCHEL ET AL. — STANFORD MARSHMALLOW STUDY</div>
        <div class="stat-big">3.2x</div>
        <p>Higher lifetime earnings were observed in adults who demonstrated high delayed gratification as children. Your psych score reflects this directly.</p>
      </div>
      <div class="science-card">
        <div class="src">KAHNEMAN & TVERSKY — PROSPECT THEORY, 1979</div>
        <div class="stat-big">2.5x</div>
        <p>People feel losses 2.5× more intensely than equivalent gains. Loss aversion is the #1 behavioral bias that prevents wealth accumulation.</p>
      </div>
      <div class="science-card">
        <div class="src">COMPOUND INTEREST — EINSTEIN (ATTRIBUTED)</div>
        <div class="stat-big">72 Rule</div>
        <p>Divide 72 by your annual return rate to find the years needed to double your money. At 8%: 9 years. At 12%: 6 years. Time is the ultimate lever.</p>
      </div>
      <div class="science-card">
        <div class="src">THOMAS J. STANLEY — "THE MILLIONAIRE NEXT DOOR", 1996</div>
        <div class="stat-big">7%</div>
        <p>Of millionaires own a luxury car. Most drive used vehicles and live below their means. Wealth is built through the gap between earning and spending.</p>
      </div>
    </div>

    <button class="restart-btn" onclick="restart()">↺ Recalculate</button>
  </div>
</section>

<script>
// ─────────────────────────────────────────
// PARTICLES
// ─────────────────────────────────────────
(function() {
  const canvas = document.getElementById('particles');
  const ctx = canvas.getContext('2d');
  let W, H, particles = [];

  function resize() {
    W = canvas.width = window.innerWidth;
    H = canvas.height = window.innerHeight;
  }

  function init() {
    resize();
    for (let i = 0; i < 70; i++) {
      particles.push({
        x: Math.random() * W,
        y: Math.random() * H,
        r: Math.random() * 1.5 + 0.5,
        dx: (Math.random() - 0.5) * 0.3,
        dy: (Math.random() - 0.5) * 0.3,
        o: Math.random() * 0.4 + 0.1
      });
    }
  }

  function draw() {
    ctx.clearRect(0, 0, W, H);
    particles.forEach(p => {
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(201,168,76,${p.o})`;
      ctx.fill();
      p.x += p.dx; p.y += p.dy;
      if (p.x < 0 || p.x > W) p.dx *= -1;
      if (p.y < 0 || p.y > H) p.dy *= -1;

      // draw connections
      particles.forEach(p2 => {
        const d = Math.hypot(p.x - p2.x, p.y - p2.y);
        if (d < 120) {
          ctx.beginPath();
          ctx.moveTo(p.x, p.y);
          ctx.lineTo(p2.x, p2.y);
          ctx.strokeStyle = `rgba(201,168,76,${0.06 * (1 - d/120)})`;
          ctx.stroke();
        }
      });
    });
    requestAnimationFrame(draw);
  }

  window.addEventListener('resize', resize);
  init();
  draw();
})();

// ─────────────────────────────────────────
// HERO COUNTERS
// ─────────────────────────────────────────
function animateCounter(el, target, suffix, duration) {
  let start = 0;
  const step = target / (duration / 16);
  const timer = setInterval(() => {
    start = Math.min(start + step, target);
    el.textContent = Math.floor(start) + suffix;
    if (start >= target) clearInterval(timer);
  }, 16);
}

setTimeout(() => {
  animateCounter(document.getElementById('counter1'), 57, '', 2000);
  animateCounter(document.getElementById('counter2'), 88, '%', 2000);
  animateCounter(document.getElementById('counter3'), 100, 'x', 2000);
}, 800);

// ─────────────────────────────────────────
// QUIZ STATE
// ─────────────────────────────────────────
let state = {
  targetWealth: 1000000,
  richType: null,
  currentAge: null,
  currentSavings: 0,
  monthlyIncome: 0,
  monthlyExpenses: 0,
  occupation: null,
  colIndex: 1.0,
  lifestyle: null,
  investmentRate: 0.08,
  psychScores: {},
  psychTotal: 0
};

const richValues = [100000,200000,300000,500000,750000,1000000,1500000,2000000,3000000,5000000,7500000,10000000,15000000,20000000,30000000,50000000,75000000,100000000,200000000,500000000,1000000000];

function updateRichSlider() {
  const v = parseInt(document.getElementById('richSlider').value);
  const idx = Math.round((v / 100) * (richValues.length - 1));
  const val = richValues[idx];
  state.targetWealth = val;
  document.getElementById('richVal').textContent = formatMoney(val);
}

function formatMoney(n) {
  if (n >= 1e9) return '$' + (n/1e9).toFixed(n%1e9===0?0:1) + 'B';
  if (n >= 1e6) return '$' + (n/1e6).toFixed(n%1e6===0?0:1) + 'M';
  if (n >= 1e3) return '$' + (n/1e3).toFixed(0) + 'K';
  return '$' + n;
}

function selectCard(el) {
  const key = el.dataset.key;
  document.querySelectorAll(`[data-key="${key}"]`).forEach(c => c.classList.remove('selected'));
  el.classList.add('selected');
  state[key] = el.dataset.val;
}

function selectScale(el) {
  const group = el.closest('.psych-scale');
  group.querySelectorAll('.scale-btn').forEach(b => b.classList.remove('selected'));
  el.classList.add('selected');
  const q = group.dataset.q;
  state.psychScores[q] = parseInt(el.dataset.score);
}

function startQuiz() {
  document.getElementById('hero').style.display = 'none';
  document.getElementById('quiz').scrollIntoView({ behavior: 'smooth' });
}

function updateProgress(step) {
  const pct = ((step - 1) / 4) * 100;
  document.getElementById('progressFill').style.width = pct + '%';
  document.getElementById('progressLabel').textContent = `STEP ${step} / 5`;
}

function nextStep(current) {
  if (!validateStep(current)) return;
  collectStep(current);

  if (current === 4) {
    computeResult();
    return;
  }

  document.getElementById(`step${current}`).classList.remove('active');
  document.getElementById(`step${current+1}`).classList.add('active');
  updateProgress(current + 1);
  window.scrollTo({ top: document.getElementById('quiz').offsetTop - 20, behavior:'smooth' });
}

function prevStep(current) {
  document.getElementById(`step${current}`).classList.remove('active');
  document.getElementById(`step${current-1}`).classList.add('active');
  updateProgress(current - 1);
}

function validateStep(step) {
  if (step === 2) {
    const age = parseInt(document.getElementById('currentAge').value);
    const income = parseFloat(document.getElementById('monthlyIncome').value);
    if (!age || age < 16 || age > 80) {
      document.getElementById('ageGroup').classList.add('error');
      document.getElementById('ageErr').style.display = 'block';
      return false;
    }
    document.getElementById('ageGroup').classList.remove('error');
    document.getElementById('ageErr').style.display = 'none';
    if (!income || income <= 0) { alert('Please enter your monthly income.'); return false; }
  }
  return true;
}

function collectStep(step) {
  if (step === 2) {
    state.currentAge = parseInt(document.getElementById('currentAge').value) || 25;
    state.currentSavings = parseFloat(document.getElementById('currentSavings').value) || 0;
    state.monthlyIncome = parseFloat(document.getElementById('monthlyIncome').value) || 3000;
    state.monthlyExpenses = parseFloat(document.getElementById('monthlyExpenses').value) || 2000;
    state.occupation = document.getElementById('occupation').value;
  }
  if (step === 3) {
    state.colIndex = parseFloat(document.getElementById('colIndex').value) || 1.0;
    state.investmentRate = parseFloat(document.getElementById('investmentType').value) || 0.08;
  }
  if (step === 4) {
    let total = 0;
    const keys = ['q1','q2','q3','q4','q5','q6'];
    keys.forEach(k => { total += state.psychScores[k] || 3; });
    state.psychTotal = total; // max 30
  }
}

// ─────────────────────────────────────────
// CALCULATION ENGINE
// ─────────────────────────────────────────
function computeResult() {
  collectStep(4);

  const annualIncome = state.monthlyIncome * 12;
  const annualExpenses = state.monthlyExpenses * 12;

  // Lifestyle multiplier
  const lifestyleMap = { frugal: 0.75, balanced: 1.0, lifestyle_creep: 1.25, yolo: 1.6 };
  const lifestyleMult = lifestyleMap[state.lifestyle] || 1.0;
  const adjustedExpenses = annualExpenses * lifestyleMult;

  // Occupation income multiplier
  const occMap = {
    tech: 1.4, finance: 1.5, business: 1.6, law: 1.35, healthcare: 1.3,
    creative: 0.9, education: 0.8, trades: 1.0, service: 0.75, other: 1.0
  };
  const occMult = occMap[state.occupation] || 1.0;
  const adjustedIncome = annualIncome * occMult;

  // CoL adjustment
  const adjustedSavings = (adjustedIncome - adjustedExpenses) / state.colIndex;
  const annualSavings = Math.max(adjustedSavings, 0);

  // Psychology mindset multiplier (affects return rate)
  const psychPct = state.psychTotal / 30; // 0 to 1
  const mindsetBonus = (psychPct - 0.5) * 0.06; // -0.03 to +0.03
  const effectiveRate = Math.max(state.investmentRate + mindsetBonus, 0.01);

  // Compound growth calculation
  // FV = PV*(1+r)^n + PMT * [((1+r)^n - 1) / r]
  // Solve for n: target = savings*(1+r)^n + annual_savings * [((1+r)^n - 1)/r]
  // Numerical iteration
  let years = 0;
  let fv = state.currentSavings;
  const target = state.targetWealth;
  const maxYears = 80;

  for (let y = 1; y <= maxYears; y++) {
    fv = fv * (1 + effectiveRate) + annualSavings;
    if (fv >= target) { years = y; break; }
  }

  if (years === 0) years = maxYears;

  const resultAge = state.currentAge + years;
  const clampedAge = Math.min(resultAge, 99);

  // Build wealth projection data
  const labels = [], myData = [], avgData = [];
  let myFV = state.currentSavings;
  let avgFV = state.currentSavings;
  const avgRate = 0.05;
  const avgAnnualSavings = Math.max(annualIncome * 0.1, 2400);

  for (let y = 0; y <= Math.min(years + 10, 50); y++) {
    labels.push(state.currentAge + y);
    myData.push(Math.round(myFV));
    avgData.push(Math.round(avgFV));
    myFV = myFV * (1 + effectiveRate) + annualSavings;
    avgFV = avgFV * (1 + avgRate) + avgAnnualSavings;
  }

  // Show results
  showResults(clampedAge, years, effectiveRate, annualSavings, psychPct, labels, myData, avgData);
}

function showResults(age, years, rate, annualSavings, psychPct, labels, myData, avgData) {
  document.getElementById('quiz').style.display = 'none';
  const results = document.getElementById('results');
  results.style.display = 'block';
  results.classList.add('visible');
  window.scrollTo({ top: 0, behavior: 'smooth' });

  // Animate age counter
  let counter = 16;
  const target = age;
  const ageEl = document.getElementById('resultAge');
  const speed = Math.max(Math.floor((target - 16) / 30), 1);
  const timer = setInterval(() => {
    counter = Math.min(counter + speed, target);
    ageEl.textContent = counter;
    if (counter >= target) clearInterval(timer);
  }, 40);

  document.getElementById('inlineAge').textContent = age;
  document.getElementById('resultHeadline').innerHTML = `You'll reach your wealth goal at age <span style="color:var(--gold)">${age}</span>`;

  const adjective = age < 35 ? 'Exceptional' : age < 45 ? 'Strong' : age < 55 ? 'Solid' : age < 65 ? 'On Track' : 'Late-stage';
  const descs = {
    'Exceptional': `You're on a lightning trajectory — top 5% of wealth accumulators. Your savings discipline, mindset score, and investment strategy create a powerful compounding engine. The gap between your income and expenses is your greatest asset.`,
    'Strong': `You're ahead of 70% of people your age. Your financial habits are working, but there's clear room to accelerate — particularly in boosting your investment rate and protecting against lifestyle inflation.`,
    'Solid': `You're following a reliable, conventional path. Most Americans never reach your goal at all. Optimizing one or two factors — especially mindset and investment rate — could shave years off significantly.`,
    'On Track': `You're building wealth steadily, but the timeline is long. The #1 lever you can pull is increasing income — not just cutting costs. Consider income diversification alongside tighter expense control.`,
    'Late-stage': `Your current trajectory requires course correction. The math is unfavorable right now — but behavioral changes are highly non-linear. A shift in savings rate of 5-10% can compress timelines dramatically.`
  };

  document.getElementById('resultDesc').textContent = descs[adjective];

  // Factor cards
  const lifestyleLabel = { frugal:'Frugal 🧘', balanced:'Balanced ⚖️', lifestyle_creep:'Lifestyle Creep 📈', yolo:'YOLO 🎰' };
  const factors = [
    { icon:'💰', title:'Annual Savings', value: '$'+Math.round(annualSavings/1000)+'K/yr', impact: annualSavings > 15000 ? 'pos' : annualSavings > 5000 ? 'neu' : 'neg', impactTxt: annualSavings > 15000 ? '↑ Strong savings rate' : annualSavings > 5000 ? '→ Moderate savings' : '↓ Low savings rate' },
    { icon:'📈', title:'Investment Rate', value: Math.round(rate*100)+'%/yr', impact: rate > 0.1 ? 'pos' : rate > 0.05 ? 'neu' : 'neg', impactTxt: rate > 0.1 ? '↑ Growth strategy' : rate > 0.05 ? '→ Moderate returns' : '↓ Underperforming' },
    { icon:'🌍', title:'Lifestyle', value: lifestyleLabel[state.lifestyle] || '—', impact: state.lifestyle === 'frugal' ? 'pos' : state.lifestyle === 'balanced' ? 'neu' : 'neg', impactTxt: state.lifestyle === 'frugal' ? '↑ High saving ratio' : state.lifestyle === 'yolo' ? '↓ Spending sabotage' : '→ Moderate impact' },
    { icon:'🧠', title:'Psych Score', value: Math.round(psychPct*100)+'/100', impact: psychPct > 0.7 ? 'pos' : psychPct > 0.45 ? 'neu' : 'neg', impactTxt: psychPct > 0.7 ? '↑ Wealth mindset' : psychPct > 0.45 ? '→ Developing' : '↓ Limiting beliefs' },
    { icon:'🏙', title:'Cost of Living', value: ['Ultra High','High','Medium','Low','Very Low'][Math.round((state.colIndex-0.5)/1.3*4)], impact: state.colIndex < 0.8 ? 'pos' : state.colIndex < 1.2 ? 'neu' : 'neg', impactTxt: state.colIndex < 0.8 ? '↑ Geo-arbitrage advantage' : state.colIndex > 1.4 ? '↓ High CoL drag' : '→ Average CoL' },
    { icon:'🎯', title:'Wealth Goal', value: formatMoney(state.targetWealth), impact: state.targetWealth < 2e6 ? 'pos' : state.targetWealth < 10e6 ? 'neu' : 'neg', impactTxt: state.targetWealth < 1e6 ? '↑ Achievable target' : state.targetWealth > 10e6 ? '↓ Ambitious timeline' : '→ Solid target' },
  ];

  const grid = document.getElementById('factorsGrid');
  grid.innerHTML = '';
  factors.forEach((f, i) => {
    grid.innerHTML += `
      <div class="factor-card" style="animation-delay:${i*0.1}s">
        <div class="card-icon">${f.icon}</div>
        <div class="card-title">${f.title}</div>
        <div class="card-value">${f.value}</div>
        <div class="card-impact ${f.impact}">${f.impactTxt}</div>
      </div>`;
  });

  // Chart
  setTimeout(() => {
    const ctx = document.getElementById('wealthChart').getContext('2d');
    new Chart(ctx, {
      type: 'line',
      data: {
        labels,
        datasets: [
          {
            label: 'Your Trajectory',
            data: myData,
            borderColor: '#c9a84c',
            backgroundColor: 'rgba(201,168,76,0.08)',
            borderWidth: 2.5,
            pointRadius: 0,
            tension: 0.4,
            fill: true
          },
          {
            label: 'National Average',
            data: avgData,
            borderColor: 'rgba(255,255,255,0.15)',
            backgroundColor: 'transparent',
            borderWidth: 1.5,
            pointRadius: 0,
            tension: 0.4,
            borderDash: [6,4]
          },
          {
            label: 'Target',
            data: labels.map(() => state.targetWealth),
            borderColor: 'rgba(46,204,113,0.4)',
            backgroundColor: 'transparent',
            borderWidth: 1,
            pointRadius: 0,
            borderDash: [2,6]
          }
        ]
      },
      options: {
        responsive: true,
        interaction: { mode:'index', intersect:false },
        plugins: {
          legend: { labels: { color:'rgba(240,234,214,0.7)', font:{ family:'Space Mono', size:11 }, boxWidth:24 } },
          tooltip: {
            backgroundColor: 'rgba(14,14,26,0.95)',
            borderColor: 'rgba(201,168,76,0.3)',
            borderWidth: 1,
            titleColor: '#c9a84c',
            bodyColor: '#f0ead6',
            callbacks: {
              label: ctx => ` ${ctx.dataset.label}: ${formatMoney(ctx.raw)}`
            }
          }
        },
        scales: {
          x: { grid: { color:'rgba(255,255,255,0.04)' }, ticks: { color:'rgba(255,255,255,0.3)', font:{size:11} } },
          y: {
            grid: { color:'rgba(255,255,255,0.04)' },
            ticks: {
              color:'rgba(255,255,255,0.3)',
              font:{size:11},
              callback: v => formatMoney(v)
            }
          }
        }
      }
    });
  }, 300);

  // Mindset score
  const mindsetPct = Math.round(psychPct * 100);
  setTimeout(() => {
    document.getElementById('mindsetFill').style.width = mindsetPct + '%';
    document.getElementById('mindsetScore').textContent = mindsetPct + ' / 100';
  }, 500);

  const traits = [];
  if (state.psychScores.q1 >= 4) traits.push('💰 Saver');
  if (state.psychScores.q1 === 5) traits.push('🚀 Multiplier');
  if (state.psychScores.q2 >= 4) traits.push('🔄 Resilient');
  if (state.psychScores.q3 >= 4) traits.push('📊 Income-focused');
  if (state.psychScores.q4 >= 4) traits.push('⏳ Patient');
  if (state.psychScores.q5 >= 4) traits.push('🛡️ Peer-immune');
  if (state.psychScores.q6 >= 4) traits.push('📚 Financial Scholar');
  if (state.psychScores.q2 <= 2) traits.push('⚠️ Avoidance pattern');
  if (state.psychScores.q5 <= 2) traits.push('⚠️ Social spending risk');
  if (traits.length === 0) traits.push('🔍 Developing');

  const traitsEl = document.getElementById('mindsetTraits');
  traitsEl.innerHTML = traits.map(t => `<span class="trait-tag">${t}</span>`).join('');
}

function restart() {
  window.location.reload();
}

// Init slider display
updateRichSlider();
</script>
</body>
</html>
